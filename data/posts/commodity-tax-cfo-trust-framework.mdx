import OptionsConsidered from '@/components/blog/framework/OptionsConsidered'
import ConstraintsBlock from '@/components/blog/framework/ConstraintsBlock'
import DecisionRationale from '@/components/blog/framework/DecisionRationale'
import Glossed from '@/components/resume/story/Glossed'
import { CommodityTaxDecisionMapFigure, CommodityTaxInvestigationFigure } from '@/components/visualizations/commodity-tax'
import VisualizationContainer from '@/components/blog/VisualizationContainer';
import { COMMODITY_TAX_PRACTITIONER_DECISIONS, COMMODITY_TAX_PRACTITIONER_INVESTIGATION } from '@/data/visualizations/commodityTax'

In September 2022 I joined <Glossed>CFO Group</Glossed> at RBC as a Senior Data Scientist. Nobody had asked me to build AI. I was hired to help automate the <Glossed>Commodity Tax</Glossed> return process, a manual workflow that consumed finance analysts for months per cycle.

By Q4 2023, the process took **90 minutes**. The metric earned the **CFO Group RBC Quarterly Team Award**. More importantly, it earned the credibility to propose Aegis, Astraeus, and eventually PAR Assist, a cascade that shaped the next three years.

This post examines two architecture decisions under explicit constraints, with the alternatives, trade-offs, and residual costs kept visible. [How Commodity Tax Built CFO Trust](/blog/commodity-tax-cfo-trust) tells the delivery story. [Commodity Tax: Making a Financial Pipeline Inspectable](/blog/commodity-tax-provenance) explains the five-stage calculation path, recorded lineage, configured inspection views, and evidence-failure cases.

<VisualizationContainer minHeight={0} caption={COMMODITY_TAX_PRACTITIONER_DECISIONS.caption} className="md:-mx-12 lg:-mx-24" variant="open">
  <CommodityTaxDecisionMapFigure content={COMMODITY_TAX_PRACTITIONER_DECISIONS} />
</VisualizationContainer>

## Constraints before choices

Five constraints shaped every decision below. Three regulatory or hard-technical, two soft-organizational. Within the evaluated options, the Tableau-as-transparency-layer architecture addressed all five together.

<ConstraintsBlock
  constraints={[
    {
      kind: 'hard',
      label: 'GL journal scale',
      detail: '~10–50 million rows per tax cycle across multiple GL systems. Incremental batching was acceptable; stateless one-pass was not.',
    },
    {
      kind: 'regulatory',
      label: 'Auditability: chain of custody',
      detail: 'Each in-scope derived number needed a recorded path back to the journal entries associated with it. Review required both output reconciliation and evidence about the automation path; lineage could support that review but could not establish source or rule correctness by itself.',
    },
    {
      kind: 'regulatory',
      label: 'Tax return compliance',
      detail: 'Returns feed statutory filings, and the process covers a roughly $600M allocation per cycle. No silent re-categorizations; rule changes require explicit approval and a version trail.',
    },
    {
      kind: 'soft',
      label: 'Tool adoption friction',
      detail: 'Finance team’s muscle memory was Tableau + Excel. A new frontend would add adoption cost on top of automation cost, and adoption cost is what kills automations on first sight of an anomaly.',
    },
    {
      kind: 'soft',
      label: 'First-impression risk',
      detail: 'New AI hire, no internal reputation. A black-box v1 that produced correct answers but couldn’t be inspected would have been worse than the manual process. The first time a number looked off, everyone would have reverted to spreadsheets.',
    },
  ]}
/>

## First project, first audition

When you join an organization as the AI person and your first assignment is process automation, there are two ways to read it. You can treat it as a detour: &ldquo;I&rsquo;ll do this so I can get to the real AI work.&rdquo; Or you can treat it as an audition.

I treated it as an audition. Finance leaders were not going to green-light a machine-learning platform for CFO-level decisions on the strength of my CV. They were going to green-light it on the strength of my first delivery.

That framing changed how I built.

## Two architectural decisions

The first decision was close to obvious. The second decision is the whole post.

### Decision 1 · Compute platform

<OptionsConsidered
  decision="Extraction pipeline for ~10–50M GL journal rows per cycle"
  options={[
    {
      name: 'PySpark on CDP',
      chosen: true,
      summary: 'Distributed dataframe on the enterprise-sanctioned platform.',
      pros: [
        'Scales to full-cycle extraction in one pass; no batching gymnastics',
        'CDP is the sanctioned platform for PII-adjacent workloads, staying inside avoids a governance fight',
        'Code is maintainable by other data scientists, not just me',
        'Retained pipeline identifiers and records can support configured lineage paths downstream',
      ],
      cons: [
        'Steeper onboarding vs. pandas for collaborators unfamiliar with Spark',
        'Local dev cycle slower than pure-Python, necessary trade for the scale property',
      ],
    },
    {
      name: 'pandas with incremental batching',
      pros: [
        'Faster local iteration',
        'Broad familiarity in the data team',
      ],
      cons: [
        'Incremental batching gymnastics for datasets of this size',
        'Memory pressure on any single machine running a full cycle',
        'Edge cases in batch boundaries become new failure modes, exactly the wrong place to introduce non-determinism',
      ],
    },
    {
      name: 'SAS (incumbent finance tooling)',
      pros: [
        'Finance team already has SAS muscle memory',
        'Some adjacent processes already use it',
      ],
      cons: [
        'Not part of the AI team’s stack, commits the future to SAS for any AI-adjacent work',
        'Licensing cost scales with team adoption',
        'Future-AI surface (Aegis, Astraeus, PAR Assist) was Python-native; SAS would have forked the stack',
      ],
    },
  ]}
/>

<DecisionRationale
  option="PySpark on CDP"
  summary="Match the data scale (hard constraint) and stay inside the sanctioned platform (regulatory)."
>
  PySpark matched the known full-cycle GL volume, and <Glossed>CDP</Glossed> was the sanctioned enterprise platform. Pandas would have required explicit batching and memory-management work at that scale, while SAS would have split this workflow from the Python stack used for the planned AI-adjacent work. The choice favored scale fit, platform fit, and maintainability.

  A subtlety worth naming: this decision didn&rsquo;t earn any stakeholder trust. Finance leadership did not care which dataframe library produced the numbers. The compute decision was about <em>making the second decision possible</em>: once compute was solved, attention could move to the architectural call that did earn trust.
</DecisionRationale>

### Decision 2 · Output architecture

This is the decision that built trust.

<OptionsConsidered
  decision="What the finance team interacts with: the output surface"
  options={[
    {
      name: 'Static report artifact',
      summary: 'Excel / PDF of the finished numbers, delivered at end of pipeline.',
      pros: [
        'Simplest implementation; matches existing manual-process hand-off pattern',
        'A familiar review surface for the finance team',
      ],
      cons: [
        'Black box: reviewers can’t inspect how a number was produced',
        'First time a number looks off, the team reverts to spreadsheets',
        'Fails the auditability constraint in spirit if not in letter',
      ],
    },
    {
      name: 'Tableau dashboard as the output',
      summary: 'Dynamic view of the finished numbers.',
      pros: [
        'Better UX than PDF',
        'Familiar tool: uses existing Tableau muscle memory',
      ],
      cons: [
        'Still a black box: dashboard shows the output, not the logic that produced it',
        'Doesn’t build the trust relationship needed for adoption',
      ],
    },
    {
      name: 'Tableau as the transparency layer',
      chosen: true,
      summary: 'Configured dashboard views cover key pipeline stages so an analyst can inspect recorded state and follow supported lineage paths. Final numbers are an emission alongside the dashboards, not their replacement.',
      pros: [
        'Configured stage views support review of extract / reconcile / map / aggregate / return records',
        'Supported drill paths let reviewers move from an aggregate toward its associated GL rows',
        'Makes investigation and reconciliation more direct than a final artifact alone',
        'Creates a shared surface for analysts and the delivery team to review anomalies',
      ],
      cons: [
        'More dashboards to build and maintain vs. a single output view',
        'Analysts can see intermediate mess early on, requires a "we’ll fix this together" framing',
      ],
    },
    {
      name: 'Custom audit UI',
      pros: [
        'Purpose-built for this exact workflow',
        'No Tableau licensing dependency',
      ],
      cons: [
        'Overengineered for v1',
        'Competing tool surface the finance team has no reason to adopt',
        'Couldn’t be maintained by the data team long-term',
      ],
    },
  ]}
/>

<DecisionRationale
  option="Tableau as the transparency layer"
  summary="The return was the deliverable; inspection made it reviewable."
>
  This is subtle but it&rsquo;s the whole thing. The finance team didn&rsquo;t need to trust me. They needed to trust the pipeline. A black-box automation that produced a correct answer would have been worse than the manual process, because the first time something looked off, everyone would revert to the spreadsheets. A transparent automation, even one with bugs in v1, built a debuggable, auditable relationship between the analysts and the system.

  In the evaluated workflow, this addressed the main constraints together: retained lineage for review, authorized inspection at configured stages, Tableau familiarity for the finance team, and a presentation layer shaped around the PySpark outputs.

  The structural reason this can work: when the pipeline retains usable, correctly associated lineage for a stage, the dashboard can reuse that relation rather than implement a second lineage path. Dashboard semantics, access controls, and performance still require separate engineering and review. <a href="/blog/commodity-tax-provenance">The technical note</a> states those assumptions and failure modes explicitly.
</DecisionRationale>

## The stakeholder-trust loop

The dashboards supported a repeatable review pattern: when an analyst questioned an aggregate, the team could inspect the recorded pipeline state together, follow a configured lineage path toward associated rows, and either reconcile the result or correct a mapping. The view did not prove that a number or rule was correct; it made the evidence and the point of disagreement easier to inspect.

<VisualizationContainer minHeight={0} caption={COMMODITY_TAX_PRACTITIONER_INVESTIGATION.caption} className="md:-mx-12 lg:-mx-24" variant="open">
  <CommodityTaxInvestigationFigure content={COMMODITY_TAX_PRACTITIONER_INVESTIGATION} />
</VisualizationContainer>

By Q4 2023, the cycle took 90 minutes and the work received the CFO Group RBC Quarterly Team Award. The shared review surface was a contributing design choice; that does not establish that one interface alone caused adoption.

## Impact

The process was reduced from months to 90 minutes per cycle. Separately, it covered a roughly $600M allocation per cycle. The work received the CFO Group RBC Quarterly Team Award (Q4 2023). Those outcomes sit outside the architecture figures: configured inspection made review more direct, but did not establish correctness by itself.

## What this opened

By the time Commodity Tax was running cleanly, the permission structure inside the CFO Group had shifted. I could propose <Glossed>Aegis v1</Glossed> (a benchmarking engine for the Big 6 Canadian banks) and get the air cover to build it. When Aegis v1 shipped (2025 CFO One RBC Team Award), I could propose <Glossed>Aegis v2</Glossed>, the text-to-SQL refactor, and deliver it in a concurrent 2-week sprint while Astraeus productionization was underway. When <Glossed>Astraeus</Glossed> reached production scale, I had the credibility to conceive <Glossed>PAR Assist</Glossed>, hand the concept to the 2025 Amplify cohort for problem-space ideation, and then build the production platform end-to-end for a full CFO Group launch across all geographies.

Every one of those projects was technically ambitious. None of them would have been greenlit by a new hire with nothing to show for it. Commodity Tax was underwriting every subsequent bet.

## What I&rsquo;d do differently

One scope call I&rsquo;d revisit: the first dashboard set surfaced provenance for *outputs* but not for *rules*. Analysts could use configured views to follow supported lineage paths from a derived number toward associated source GL rows, but the rules that produced each category mapping lived in PySpark code, modifiable only by the data team. New tax categories or re-categorizations of existing GL accounts therefore required code changes and redeploys.

In retrospect I&rsquo;d have built a *mapping-rules review surface* earlier, letting analysts propose rule additions while keeping code review and production approval explicit. The first dashboard set exposed evidence for outputs, but not the rule set that produced each category mapping. Giving reviewers a governed way to inspect proposed rule changes could have reduced coordination and redeployment work without turning a dashboard edit into an automatic production change.

## The pattern

Your first project at a new organization isn&rsquo;t just a deliverable. It&rsquo;s the audition that determines which doors open next.

The architectural decision (Tableau as a transparency layer, not an output) was the first time I applied a principle I&rsquo;d use in every subsequent system: **the hardest thing in production AI isn&rsquo;t getting the answer right; it&rsquo;s getting the humans who own the decision to trust the system that produced it.**

Astraeus&rsquo;s LLM-as-Router separation is a descendant of this idea. Aegis v2&rsquo;s confidence-thresholded clarification is a descendant of this idea. PAR Assist&rsquo;s registered tool path writes structured records for successful field-assignment calls. In regulated production systems, the architecture has to support inspection, testing, and stakeholder review.

Commodity Tax was where I learned that lesson. The technology changes. The pattern doesn&rsquo;t.
